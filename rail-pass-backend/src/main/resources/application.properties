# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/railpass_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=roshan412

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Server Port
server.port=${PORT:8081}

# Error Handling
server.error.include-message=always
server.error.include-stacktrace=never

# JWT Secret
jwt.secret=${JWT_SECRET:your-very-secure-and-long-secret-key-for-railpass-system-256-bit}

# CORS Allowed Origins
cors.allowed-origins=${FRONTEND_URL:http://localhost:5173,http://localhost:3000}

